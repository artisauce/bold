#include "map.hpp"

map::map(unsigned int seedInput, size_t mapSideInput, size_t tileSideInput, size_t battlefieldSideInput):
	seed(seedInput),
    mapSide(mapSideInput),
    tileSide(tileSideInput),
    battlefieldSide(battlefieldSideInput)
{

}